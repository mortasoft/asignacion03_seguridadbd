version: '3.8'
services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Seguridad_2026! # Debe tener Mayus, minus, num y símbolo
    ports:
      - "1433:1433"

  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
